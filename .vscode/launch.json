{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Debug philosophers",
			"type": "lldb",
			"request": "launch",
			"program": "${workspaceFolder}/cub3d_bonus",
			"args": ["${workspaceFolder}/maps/bonus_door.cub"],
			"cwd": "${workspaceFolder}",
			"console": "integratedTerminal",
			"preLaunchTask": "build"
		}
	]
}